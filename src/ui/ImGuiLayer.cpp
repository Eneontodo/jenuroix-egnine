// ImGuiLayer.cpp
// This file provides the ImGui backend implementations.
// Must be compiled exactly ONCE.

#include "ui/ImGuiLayer.h"

// ImGui backends are header-only implementations — they need one .cpp
// They are included here so the linker finds them.
// (backends are in vendor/imgui/backends/)
